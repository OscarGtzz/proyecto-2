<% include partials/_header %>

<div class="card">
  <div class="card-body">
    <form action="/edit/<%= task._id %>" method="post">
      <div class="form-group">
        <input class="form-control" type="text" name="usuario" placeholder="Usuario" value="<%= task.usuario %>">
      </div>
      <div class="form-group">
        <input class="form-control" type="text" name="fecha" placeholder="Fecha" value="<%= task.fecha %>">
      </div>
      <div class="form-group">
        <textarea class="form-control" name="actividad" cols="80"
          placeholder="Actividad"
          ><%= task.actividad %></textarea>
      </div>

      <button class="btn btn-primary" type="submit">
        Actualizar 
      </button>
    </form>
  </div>
</div>     

<% include partials/_footer%>
